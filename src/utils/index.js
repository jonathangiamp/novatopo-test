const formatInputData = require('./formatInputData');
const formatOutputData = require('./formatOutputData');
const isObjectEmpty = require('./isObjectEmpty');
const readInputFile = require('./readInputFile');
const writeInputFile = require('./writeInputFile');

module.exports = {
  formatInputData,
  formatOutputData,
  isObjectEmpty,
  readInputFile,
  writeInputFile,
};
